name: Publish to DeployLog
on:
    release:
        types: [published] # the real trigger the product sells
jobs:
    publish:
        runs-on: ubuntu-latest
        steps:
            - uses: deploylogdev/action@v1
              with:
                  api-key: ${{ secrets.DEPLOYLOG_API_KEY }}
                  project: deploylog
                  notify-subscribers: true
